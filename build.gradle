plugins {
    id 'org.springframework.boot' version '2.5.2'
    id 'io.spring.dependency-management' version '1.0.11.RELEASE'
    id 'java'
    id 'war'
    id "maven-publish" // Nexus plugin
    id "org.sonarqube" version "2.7" // Sonar plugin
    id "checkstyle"
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '1.8'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    providedRuntime 'org.springframework.boot:spring-boot-starter-tomcat'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}

// Checkstyle SAST tool for compliant with specified coding rules
checkstyle {
  toolVersion = '6.16.1'
  config = rootProject.resources.text.fromFile('checkstyle/rules/rules.xml')
  ignoreFailures = true
}

// SonarQube Code Analysis Config
sonarqube {
    properties {
        property "sonar.sourceEncoding", "UTF-8"
                property "sonar.projectName", "Gradle-JavaWebApp-3"
                property "sonar.host.url", "http://34.229.136.79:9000"
                property "sonar.login", "5daa556db9f1df4d09c8301e0929a4738d1a6ba7"
    }
}

// Nexus Upload
publishing {
    publications {
        maven(MavenPublication) {

            // bootJar is the default build task configured by Spring Boot
            artifact bootJar
        }
    }
    repositories {
        maven {
            url = "http://172.31.90.171:8081/repository/gradle-java-webapp-Repo/"
            credentials {
                username "admin"
                password "admin"
            }
        }
    }
}
